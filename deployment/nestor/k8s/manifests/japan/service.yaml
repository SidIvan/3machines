apiVersion: v1
kind: Service
metadata:
  labels:
    app: nestor-japan
  name: nestor-japan
  annotations:
      prometheus.io/scrape: 'true'
      prometheus.io/port:   '9001'
spec:
  ports:
  - port: 9001
  selector:
    app: nestor-japan