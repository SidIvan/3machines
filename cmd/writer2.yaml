global.db.batch.size:
  10000
get.full.snapshot.period.m:
  10000
full.snapshot.depth:
  5000
reconnect.period.m:
  2
ex.info.upd.per.m:
  5

local.repo.config:
  delta.collection.name:
    deltas
  snapshot.collection.name:
    snapshots
  exchange.info.collection.name:
    exchange_info
  book.ticker.collection.name:
    binance_order_books_tops
  mongo:
    timeout.sec:
      1
    database.name:
      db
    num.conn.retries:
      3
    uri:
      schema:
        mongodb://
      host:
        mongodb
      port:
        27017

binance.client:
  delta.stream.url:
    schema:
      wss://
    host:
      stream.binance.com
    port:
      9443
  http.base.uri.config:
    schema:
      https://
    host:
      api.binance.com
    port:
      443

global.repo.config:
  base.uri:
    host:
      176.113.80.155
    port:
      9000
  timeoutS:
    3
  database.name:
    db
  delta.table.name:
    tmp_binance
  snapshot.table.name:
    tmp_binance_snapshots
  ex.info.table:
    exchange_info
  book.ticker.table:
    binance_order_books_tops