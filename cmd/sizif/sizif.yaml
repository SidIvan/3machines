parquet.storage:
  base.path: "3machines"

global.repo.config:
  ch.pool:
    base.uri:
      host:
        91.198.220.162
      port:
        10360
    user:
      sizif
    password:
      KPCxZ88Ln6SB
    dial.timeout.s:
      5
    read.timeout.s:
      5
    ch.max.conns:
      3
    ch.min.conns:
      1
  database.name:
    binance
  delta.table.name:
    deltas

num.parallel.processes:
  4

process.deltas.from:
  2024-08-31T12:00:00

delete.processed.deltas:
  false